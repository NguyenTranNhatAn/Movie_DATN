import React from 'react';
import { View, Text } from 'react-native';

const HomeScreens = ({navigation}) => {
  return (
    <View>
      <Text>HomeScreens</Text>
    </View>
  );
};

// Đảm bảo rằng bạn đang export đúng cách
export default HomeScreens;